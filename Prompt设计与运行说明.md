# 招聘岗位结构化抽取：Prompt 设计与稳定性优化文档

## 1. 实验目标

本实验比较三类 Prompt 在金融科技招聘岗位结构化抽取任务中的表现，重点检验以下问题：

1. 增加 JSON Schema、缺失值规则和证据约束，能否提高标量字段准确率并降低幻觉；
2. 增加抽取后的静默自检，能否提高格式合规性和重复运行稳定性；
3. 精度、列表召回、稳定性、速度和成本之间是否存在权衡；
4. 是否应当选择一个 Prompt 覆盖所有任务，还是采用分层路由。

实验版本为 `440-v2`。输入为岗位结构化字段与证据摘要，不包含完整职位描述；参考答案为规则生成并待人工复核的银标准。

## 2. 数据与实验设计

- 原始岗位表：440 行。
- 有效岗位：439 条；剔除 1 条“未找到岗位”占位记录。
- Benchmark：88 条，AI 相关与非 AI 岗位各 44 条。
- Prompt：P1、P2、P3。
- 重复：每个岗位与 Prompt 组合重复 3 次。
- 调用规模：88 × 3 × 3 = 792 次。
- 模型：`deepseek-v4-flash`。
- 温度：0。
- 最大输出长度：1200 tokens。
- 随机种子：`20260815`。
- API Key 环境变量：`DEEPSEEK_API_KEY`。

三组 Prompt 使用相同模型、温度、最大输出长度、Benchmark 和重复次数，唯一主要自变量是 Prompt 结构。

## 3. 统一抽取字段

基础评价字段为：

```json
{
  "job_title": "string|null",
  "salary": "string|null",
  "location": "string|null",
  "hard_skills": ["string"],
  "soft_skills": ["string"],
  "education": "string|null",
  "experience": "string|null",
  "ai_stack": ["string"]
}
```

其中：

- 标量字段缺失时使用 `null`；
- 列表字段缺失时使用 `[]`；
- `hard_skills`、`soft_skills` 和 `ai_stack` 均按集合评价；
- P2 和 P3 额外包含证据或自检字段，但公共评价字段保持一致。

## 4. P1：直接零样本 JSON 抽取

### 4.1 设计定位

P1 是基础基线，只提出抽取任务和 JSON 输出要求，不提供字段级证据约束、详细规范化规则或自检步骤。其作用是测量“最简 Prompt”在现有结构化证据摘要上的效率和基础表现。

### 4.2 System Prompt

```text
你是招聘信息结构化助手。请从用户提供的招聘文本中提取信息，并只输出一个合法的JSON对象。缺失信息使用null或空数组。
```

### 4.3 User Prompt 模板

```text
请提取job_title、salary、location、hard_skills、soft_skills、education、experience、ai_stack八个字段。列表字段输出字符串数组。招聘文本：
{{SOURCE_TEXT}}
```

### 4.4 预期优势与风险

- 优势：指令短、输入 Token 少、平均耗时低，列表字段相对不容易因过度保守而大量留空。
- 风险：没有逐项证据约束，可能产生常识补全；没有严格 Schema 兜底；地点和技能规范化不足。

## 5. P2：Schema 约束与证据抽取

### 5.1 设计定位

P2 在 P1 基础上加入统一 Schema、严格缺失值策略、禁止常识补全、字段级证据和规范化规则。设计目标是提高可核验性，降低幻觉，适用于金融合规或“宁缺毋滥”的字段。

### 5.2 System Prompt

```text
你是审慎的招聘信息抽取器。只能使用输入文本明确出现的内容，禁止依据常识补全。必须输出合法JSON，不要解释。标量缺失用null，列表缺失用[]。
```

### 5.3 User Prompt 模板

```text
从下列招聘文本提取统一JSON：{"job_title":string|null,"salary":string|null,"location":string|null,"hard_skills":string[],"soft_skills":string[],"education":string|null,"experience":string|null,"ai_stack":string[],"evidence_map":object}。规则：1) 地点中删除经验/学历尾缀；2) 薪资保留原文；3) 技能和AI技术栈必须在原文中有直接证据；4) evidence_map为每个非空字段提供不超过20字的原文短证据；5) 不确定即留空。
招聘文本：
{{SOURCE_TEXT}}
```

### 5.4 预期优势与风险

- 优势：证据边界最严格；幻觉风险低；字段类型明确；便于审计。
- 风险：证据摘要较短时可能过度留空，形成高精度、低召回；证据映射增加输出长度和耗时。

## 6. P3：抽取—核验双阶段自检

### 6.1 设计定位

P3 在严格证据约束基础上加入“候选抽取—逐项回证—Schema 复核”的内部自检，并要求输出 `unsupported_removed` 和 `self_check`。设计目标是提高稳定性、格式合规性和异常可追踪性。

### 6.2 System Prompt

```text
你是高精度招聘信息抽取与核验器。先在内部识别候选值，再逐项回到原文核验；删除无法由原文直接支持的值。不要输出推理过程，只输出一个合法JSON对象。
```

### 6.3 User Prompt 模板

```text
目标JSON：{"job_title":string|null,"salary":string|null,"location":string|null,"hard_skills":string[],"soft_skills":string[],"education":string|null,"experience":string|null,"ai_stack":string[],"evidence_map":object,"unsupported_removed":string[],"self_check":{"all_supported":boolean,"missing_fields":string[]}}。执行规则：①只提取原文明确内容，禁止推断；②标量缺失为null、列表缺失为[]；③地点只保留地理位置，剔除经验/学历；④薪资保留原文；⑤技能统一为最短明确名称并去重；⑥AI技术栈仅限模型、框架、算法、AI工程方法；⑦每个非空字段都必须在evidence_map提供原文短证据；⑧输出前静默复核JSON类型、证据支持和重复项。
招聘文本：
{{SOURCE_TEXT}}
```

### 6.4 预期优势与风险

- 优势：逐项回证和 Schema 复核有利于减少异常、重复项和格式错误；输出更容易进入人工复核流程。
- 风险：指令和输出结构最长，Token 使用及平均耗时最高；过度自检也可能压低召回。

## 7. 三组 Prompt 的设计差异

| 设计维度 | P1 直接抽取 | P2 证据约束 | P3 双阶段自检 |
|---|---|---|---|
| JSON 要求 | 合法 JSON | 明确统一 Schema | Schema + 自检字段 |
| 缺失值规则 | 概括说明 | 标量 `null`、列表 `[]` | 同 P2，复核后仍不确定则留空 |
| 证据约束 | 无 | 每个非空字段提供短证据 | 逐项证据并记录被删除候选 |
| 规范化 | 未细化 | 地点和薪资规则 | 地点、技能、AI 栈均有明确规范 |
| 自检 | 无 | 字段级证据约束 | 候选抽取—回证—Schema 复核 |
| 主要目标 | 速度与召回 | 精度与幻觉控制 | 稳定性与异常复核 |

## 8. 评价指标

### 8.1 标量准确率

岗位名称、薪资、地点、学历和经验标准化后逐字段精确匹配，再计算五个字段的平均值。

### 8.2 List F1

对参考列表非空的 `hard_skills`、`soft_skills`、`ai_stack` 计算集合级 F1：

```text
Precision = TP / 预测项数
Recall    = TP / 参考项数
F1        = 2 × Precision × Recall / (Precision + Recall)
```

参考列表为空时不计作 F1=1，而是通过幻觉率约束模型是否凭空输出内容。

### 8.3 幻觉率

```text
幻觉率 = 无法由输入文本或参考答案支持的非空原子值数 / 全部预测原子值数
```

### 8.4 稳定性

对同一岗位、同一 Prompt 的三次有效输出转为字段—值原子集合，计算三组两两 Jaccard 相似度并取均值。

### 8.5 Schema 合规率

输出能够解析为 JSON，且公共字段存在并满足标量/列表类型要求的运行比例。

### 8.6 综合分

实验脚本使用以下预设权重：

```text
综合分 = 100 × (
  0.30 × 标量准确率
  + 0.20 × List F1
  + 0.25 × (1 - 幻觉率)
  + 0.15 × 稳定性
  + 0.05 × Schema合规率
  + 0.05 × 速度分
)
```

速度分为最快 Prompt 平均耗时除以当前 Prompt 平均耗时。

## 9. Benchmark 结果

| 排名 | Prompt | 标量准确率 | List F1 | 幻觉率 | 稳定性 | Schema 合规率 | 平均耗时 | 综合分 |
|---:|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | P1 直接抽取 | 89.39% | **28.30%** | 2.04% | 98.65% | 99.62% | **1.112 秒** | **81.75** |
| 2 | P3 双阶段自检 | 93.86% | 27.15% | 0.81% | **98.84%** | **100%** | 1.635 秒 | 81.62 |
| 3 | P2 证据约束 | **95.83%** | 14.47% | **0.20%** | 98.48% | **100%** | 1.459 秒 | 80.18 |

运行完整性：

- 计划调用：792；实际记录：792；成功：792；失败：0。
- P1、P2、P3 各 264 条记录。
- Schema 合规：791/792；唯一不合规记录来自 P1。
- `benchmark_runs.jsonl` 中无重复 `run_id`。
- 三轮重复均为 264 条记录。
- 结果文件未记录或回显 `DEEPSEEK_API_KEY` 的值。

## 10. 结果解释

### 10.1 P1 为预设综合分获胜者

P1 以 81.75 分排名第一，主要得益于最快速度和最高 List F1。其 Schema 合规率为 99.62%，说明简单 Prompt 在结构化证据摘要上具有较高性价比，但仍需增加格式校验兜底。

### 10.2 P2 最保守，但列表召回不足

P2 的标量准确率最高、幻觉率最低、Schema 合规率为 100%，说明字段级证据约束有效降低了无依据输出。但 P2 的 List F1 只有 14.47%，反映证据摘要较短时模型大量留空。这不是简单的“效果最差”，而是高精度、低召回的保守策略。

### 10.3 P3 适合异常复核

P3 综合分仅比 P1 低 0.13 分，并取得最高稳定性和 100% Schema 合规率。但其平均耗时最长，更适合作为异常样本或高风险字段的二次核验，而不是所有岗位的默认 Prompt。

## 11. 最终 Prompt 路由

不采用“一个 Prompt 覆盖全部任务”，而采用风险分层路由：

1. **P1 主抽取**：默认批量抽取，速度快、List F1 相对较高；输出后增加确定性 Schema 校验。
2. **P2 证据闸门**：用于风控、合规或要求“宁缺毋滥”的字段，优先控制幻觉。
3. **P3 异常复核**：仅在字段冲突、证据不足、Schema 异常或人工抽检触发时运行。

该设计将 Prompt 工程从“寻找唯一最佳提示词”转化为“根据业务风险配置模型行为”。

## 12. 有效性边界

1. Benchmark 的参考答案是银标准，不是独立双人标注的金标准。
2. 输入为结构化字段和证据摘要，不包含完整职位描述，因此指标不能直接等同于完整 JD 的绝对抽取准确率。
3. List F1 实际可评价运行数为 108：`hard_skills` 60 次、`ai_stack` 69 次、`soft_skills` 0 次。软技能字段没有 List F1 证据。
4. 温度为 0 不意味着输出绝对确定；重复实验仍观察到少量输出差异。
5. 招聘数据是公开信息的时间快照，不能据此推断企业不存在未公开岗位或能力。
6. 本轮未运行 439 条岗位的 `full` 模式，不提供全量抽取结论。

## 13. 复现命令

离线检查已有结果：

```bash
python verify_results_440_v2.py
```

预检：

```bash
python prompt_experiment_runner_440_v2.py --mode preflight
```

重新运行 Benchmark（会产生 API 费用）：

```bash
python prompt_experiment_runner_440_v2.py --mode benchmark --repeats 3 --output-dir runs_440_v2_reproduction
```

API Key 只通过环境变量 `DEEPSEEK_API_KEY` 提供，不得写入仓库。

